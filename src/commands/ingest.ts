// src/commands/ingest.ts — Import raw material into the knowledge base
// Placeholder: full implementation in Phase 2

import type { GlobalOptions } from '../cli.js';

export async function ingestCommand(source: string, _opts: GlobalOptions): Promise<void> {
  console.log(`ingest: not yet implemented (Phase 2). Source: ${source}`);
}
