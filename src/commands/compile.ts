// src/commands/compile.ts — Deep compile: cross-source synthesis
// Placeholder: full implementation in Phase 2/4

import type { GlobalOptions } from '../cli.js';

export async function compileCommand(_opts: GlobalOptions): Promise<void> {
  console.log('compile: not yet implemented (Phase 2)');
}
