// src/commands/query.ts — Natural language query against the wiki
// Placeholder: full implementation in Phase 3

import type { GlobalOptions } from '../cli.js';

export async function queryCommand(question: string, _opts: GlobalOptions): Promise<void> {
  console.log(`query: not yet implemented (Phase 3). Question: ${question}`);
}
