// src/commands/lint.ts — Wiki health check
// Placeholder: full implementation in Phase 4

import type { GlobalOptions } from '../cli.js';

export async function lintCommand(_opts: GlobalOptions): Promise<void> {
  console.log('lint: not yet implemented (Phase 4)');
}
